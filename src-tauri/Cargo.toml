[package]
name = "some-matrix-frontend"
version = "0.1.0"
description = "A Tauri App"
authors = ["you"]
edition = "2021"

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html

[lib]
# The `_lib` suffix may seem redundant but it is necessary
# to make the lib name unique and wouldn't conflict with the bin name.
# This seems to be only an issue on Windows, see https://github.com/rust-lang/cargo/issues/8519
name = "some_matrix_frontend_lib"
crate-type = ["staticlib", "cdylib", "rlib"]

[build-dependencies]
tauri-build = { version = "2", features = [] }

[dependencies]
tauri = { version = "2", features = [] }
tauri-plugin-opener = "2"
serde = { version = "1", features = ["derive"] }
serde_json = "1"
reqwest = { version = "0.13.2", features = ["json"] }
tauri-plugin-log = "2"
log = "0.4.29"
anyhow = "1.0.102"
colored = "3.1.1"
chrono = "0.4.44"
url = "2.5.8"
percent-encoding = "2.3.2"
version-compare = "0.2.1"
matrix-sdk-crypto =  "0.16.0"
matrix-sdk-sqlite = { version = "0.16.0", features = ["crypto-store", "bundled"] }
keyring = { version = "3.6.3", default-features = false, features = ["async-secret-service", "async-io", "crypto-rust"] }
ruma = "0.14.1"
hex = "0.4.3"
getrandom = "0.4.2"
http = "1.4.0"
bytes = "1.11.1"
sha2 = "0.11.0"
bs58 = "0.5.1"
hkdf = "0.13.0"
base64 = "0.22.1"
aes = "0.9.0"
hmac = "0.13.0"
ctr = "0.10.0"
