mod app;
mod components;
mod hooks;
mod state;
mod tauri;
mod tauri_functions;

use app::*;
use leptos::prelude::*;

fn main() {
    console_error_panic_hook::set_once();
    mount_to_body(|| {
        view! {
            <App/>
        }
    })
}
